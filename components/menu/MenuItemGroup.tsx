import * as React from "react";
import { MenuItemGroupTypes } from "./types/MenuItemGroupTypes";

const MenuItemGroup: React.FC<MenuItemGroupTypes> = (
  props: MenuItemGroupTypes
): React.ReactElement => {
  return <></>;
};
export default React.memo(MenuItemGroup);
export { MenuItemGroupTypes };
