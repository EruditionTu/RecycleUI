import React from 'react';
import type { FC, ReactElement } from 'react';
import PopoverProps from './type';

const Popover: FC<PopoverProps> = (props: PopoverProps): ReactElement => {
  return <></>;
};

export default Popover;
